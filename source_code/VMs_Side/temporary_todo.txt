Sorted by importance

Project:
* Rebuild the client code, use 3 classes: Client socket, GUI, Main class (connecting between the client and GUI classes)
  * Add: `class ItemsList(QListView)` that will serve items list managing.
  * Add: consistent connection between server and client to make the server send alerts to the client,
         and whenever the client recieves a new message, add the data to the list view.
* Think about the ip the anti viruses and the clients connect to. on client VMs, the ip should not be localhost

* printing, __str__, SocketID
